# 日報 - 2026-05-25 (日)

## 作業者
グエン・ミン・チ (solo founder / dev)

## 担当
S2 Day 6 — 翻訳精度ベンチのデバイス実走、`--s2` 採点ツール作成、Day 6 クローズ判断。

## 本日の作業時間
約 2 時間 (デバイス実走＋ツール実装＋レポート) (self-reported)

## 本日の作業内容

### 1. S2-Run-30 デバイス実走
Galaxy S10 + BLE グラス、バックエンドは Day 3 revert の whisper-1（EC2 大阪）で
S2-Run-30 を実走。CSV `docs/reports/s2_accuracy_2026-05-25.csv`（60 行）を取得。
- ユニーク 54/60 句、リトライ 6 句。6 句は 60 行キャップに押し出されてクリーン行なし
  (`ja-greet-03`, `ja-emerg-04`, `vi-dir-05`, `vi-transit-01`, `vi-pay-01`, `vi-emerg-05`)。

### 2. `--s2` 採点ツールを `tools/latency_report.py` に追加
no-duplicate ルールに従い新規ファイルを作らず、既存 `latency_report.py` に `--s2`
モードを追加。`ble_ack_ms - audio_ms` 計算を `_system_latency_ms` ヘルパーに抽出して
`--s1` と共用。
- 出力: 方向別（ja->vi / vi->ja）＋ドメイン別の `% >= 4`、方向別 system latency
  パーセンタイル、retry/coverage サマリ、accuracy＋latency 両ゲートの GO/NO-GO。
- `accuracy_score` 未記入時は accuracy を PENDING にし、latency だけ先に出す設計。
- S2 テスト 5 件追加（PENDING / 両方向 PASS->GO / 片方向 FAIL->NO-GO /
  latency FAIL->NO-GO / retry カウント）。合計 9/9 green。コミット `37dd796`（push 済み）。

### 3. レイテンシ結果（criterion #2 のデバイス確認）
| 方向 | p50 | p95 | max |
|---|--:|--:|--:|
| ja->vi | 1195 | 1417 | 1434 |
| vi->ja | 1131 | 1639 | 1737 |
| **全体** | 1165 | **1449** | 1737 |

p95 1449ms < ゲート 2000ms → **PASS**（余裕 ~550ms）。2000ms に達した句はゼロ。
vi->ja の裾がやや長い（p95 1639）が安全圏。whisper-1 revert は健全、STT 詰まりなし。

### 4. Day 6 クローズ判断 — accuracy を S3 へ繰り延べ
バイリンガル単独の自己採点は信頼性が低く、かつ privacy ルールで翻訳テキストを
後採点用に保存できないため、accuracy ゲート（exit criterion #4）全体を S3 パイロット
（有償ネイティブ VN レビュアー）へ繰り延べる判断にした。Day 6 実走は criterion #2
（latency）のデバイス確認としては PASS。
- `docs/reports/S2_accuracy_2026-05-25.md` に判断＋latency 結果を記録。
- `docs/codex/S2_TASKS.md` の Day 6 行・criterion #4・open question #1 を更新。
- `.gitignore` に Python セクション（`__pycache__/`, `*.pyc`）追加。

## 本日のコミット

| Hash | 内容 |
|---|---|
| `37dd796` | feat(tools): S2 Day 6 translation-accuracy report mode (push 済み) |
| (本コミット) | docs(s2): Day 6 latency confirmed, accuracy deferred to S3 |

## 課題・懸念

- 6 句がカバレッジから漏れたが、accuracy を S3 に繰り延べたため本走では非該当。
  S3 レビュアーセッションでフルカタログを再走する。
- exit criterion 残り: #3 retry rate（Day 9 final bench）、#5 TLS Full(Strict)
  （Day 7-8）。#1 STT は No-Go 確定、#2 latency と #4 accuracy(S3 繰延) は確定。

## 次回の予定 (2026-05-26〜)

- Day 7: Cloudflare origin cert 方針（Let's Encrypt で確定済み）に沿って
  `infra/ec2/bootstrap.sh` の nginx リバースプロキシ stub をロック、runbook 起こし。
- Day 8: origin TLS デプロイ＋Cloudflare を Full(Strict) へ切替、521 が出ないことを確認。
