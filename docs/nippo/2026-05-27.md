# 日報 2026-05-27（S3 Day 2）

## 本日の作業

S3 Day 2 — OCR 用「翻訳のみ」パスの契約確定と実装（prep+impl）。

### 決定（lock）: REST `POST /v1/translate`（新規 WS メッセージ型ではない）

オープン課題 #3（REST vs WS）を Day 2 で確定。理由:

- OCR は端末側（ML Kit）で全文を認識済み → 一発の request/response が自然。音声パスのような状態機械（`session.start → audio.chunk* → audio.end`）は不要。
- REST にすることで、latency ゲートで固定済みの **音声 WS 状態機械に一切触れない**（回帰リスクゼロ）。`ws-events.schema.json` も変更なし。
- Realtime `Translator` を新メソッド `translate_text()` で再利用 → モデル・JP↔VN プロンプト・usage 解析が single source。
- レイテンシ余裕大: STT（約 880ms）と音声アップロード（約 300ms）が消えるため、connect(約300)+translate(約400)+BLE(約150) ≈ 850ms。ゲート 1500ms に十分収まる見込み（Day 6 で実測）。
- 旧 `/api/v1/ocr`（画像アップロード版）は **supersede**。端末側 OCR の方針上、画像バイトを送る実装はプライバシー境界違反になるため openapi.yaml で deprecated 明記、実装しない。

### 実装（live OpenAI パスのため stub 不可 → Claude が本体実装、Codex はテストのみ）

- `backend/app/services/translator.py`: `translate_text(text) -> TextResult` 追加。`conversation.item.create`(input_text)+`response.create` を送り、`response.output_text.*`/`response.done` を drain。`SYSTEM_INSTRUCTIONS` を音声＋テキスト両対応に一般化。
- `backend/app/api/translate.py`（新規）: `POST /v1/translate`。daily-cap 超過は 429、translator 失敗は 502。usage があれば request id 単位で cost 記録。リクエスト本文・翻訳結果は **一切ログ出力しない**。
- `backend/app/main.py`: translate router を登録。
- `docs/api-contract/openapi.yaml`: `/v1/translate` + `TranslateRequest`/`TranslateResponse` 追加、`/api/v1/ocr` を deprecated 化。
- `docs/codex/S3_TASKS.md`: Day 2 を DONE 化、課題 #3 を RESOLVED 化。
- `docs/codex/PROMPTS.md`: 「S3 Day 2 — tests only」プロンプト追加。

### 検証

- `py_compile` で構文 OK。ruff/pytest はローカル未導入のため Codex CI 側で実行。

## コミット

- `7ad32f0 feat(backend): S3 translate-only path for OCR`（**ローカルのみ、未 push**）

## 申し送り

- 次は Codex に「S3 Day 2 tests only」プロンプトを投げる（PROMPTS.md 参照）。マージ後 Day 3（モバイル OCR キャプチャ）へ。
- オペレーター宿題（S2 から継続、S3 ブロッカーではない）: 公開済み CF DNS API トークンの rotate、TLS 24h ウィンドウ確認。
- push / EC2 デプロイ / 実機はオペレーター権限。本日のコミットは未 push。

---

## 追記（同日）— Day 2 クローズ + Day 3 prep

### Day 2 クローズ（PR #16 マージ）

- 上記「未 push」は誤り。`7ad32f0`/`8892bad` は push 済みだった。ruff も backend/.venv に同梱（0.15.13）でローカル実行できた。
- Codex の tests-only PR #16 をレビュー → 承認。translate_text の各ケース + エンドポイント（200/429/502/422 + プライバシー assertion）、35 passed、scope 厳守。
- ruff 0.15 の format drift（module docstring 後の空行）がリポジトリ全体で5ファイル発生（既存の sessions.py/cost_logger.py 等含む）。PR とは無関係なので別途 `style(backend)` コミット `731d425` で解消（`test_translator.py` は PR 側がクリーンなので除外しコンフリクト回避）。
- push（`731d425`）→ PR を ready 化 → merge（`2e3c5be`）。マージ後の main で `ruff check`/`ruff format --check` green、`pytest` 35 passed 確認。**Day 2 完了。**

### Day 3 prep（モバイル OCR、prep+impl）

- サーフェス確定: `lib/ocr/ocr_scanner.dart` が **認識 + プライバシー削除** を所有。`OcrScanner.recognise(imagePath)` が端末側 ML Kit を実行し、`finally` で画像を必ず削除（失敗時も）。`recorder.dart` と同型（注入可能な `OcrDriver` で「常に削除」ロジックを単体テスト可能、ML Kit/ファイル呼び出しは不可）。
- カメラプレビュー + キャプチャ UI（`CameraController` 所有）はキャプチャ画面側 = Codex の担当（UI + 実機、単体テスト不可、Day 5 smoke で検証）。
- `recognise()` は live ML Kit パスのため本体を Claude 実装（stub だとテスト通過でも実機で落ちる）。元計画の「Codex が recognise 実装」を override。Codex はキャプチャ UI + `OcrScanner` テスト。
- 依存追加: `camera 0.11.2+1`, `google_mlkit_text_recognition 0.13.1`（`google_mlkit_commons 0.8.1`）。権限: Android `CAMERA`+`uses-feature`、iOS `NSCameraUsageDescription`。
- 検証: `flutter pub get` OK、`flutter analyze` clean（既知の pre-existing warning 2件のみ）。

### コミット（追記分）

- `731d425 style(backend): ruff format`（push 済み、PR #16 マージに内包）
- `2e3c5be Merge pull request #16`（origin/main）
- `2e4cf7d feat(mobile): S3 on-device OCR scanner + camera permission`

### 申し送り（更新）

- 次: Codex に「S3 Day 3」プロンプト（PROMPTS.md）を投げる → キャプチャ UI + テスト → マージ後 Day 4（OCR text -> translate -> BLE 配線 + OCR レイテンシ記録）。
- オペレーター宿題は据え置き（CF トークン rotate、TLS 24h）。
